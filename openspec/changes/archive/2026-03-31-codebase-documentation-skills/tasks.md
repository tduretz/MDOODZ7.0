## 1. Foundation Skills

- [x] 1.1 Create `.github/skills/skill-build-and-run/SKILL.md` — YAML frontmatter (name, description optimised for build/compile/run/dependency queries) + structured sections covering: CMake configuration, make targets (`build`, `build-dev`, `run`, `clean`), dependency installation (SuiteSparse, HDF5, BLAS/LAPACK, zlib), `env.cmake` setup, OpenMP parallelisation, and common troubleshooting. Reference: `CMakeLists.txt`, `Deps.cmake`, `makefile`, `env.cmake.example`.
- [x] 1.2 Create `.github/skills/skill-model-setup/SKILL.md` — YAML frontmatter (description targeting simulation setup/configuration/scenario queries) + sections covering: the `.c`/`.txt` paired-file pattern, all callback function signatures (`SetPhase`, `SetTemperature`, `SetGrainSize`, `SetSurfaceZCoord`, `SetHorizontalVelocity`, `SetVerticalVelocity`, `SetBCVx`, `SetBCVz`, `SetBCPType`, `SetBCT`, `SetDualPhase`, `SetPorosity`, `SetDensity`, `SetXComponent`, `SetPressure`, `SetNoise`, `SetAnisoAngle`, `SetDefGrad`, `MutateInput`), the `Coordinates` struct, the `MdoodzSetup` struct, the `POSITION` enum, BC type codes, `.txt` file sections (`RESTART`, `SCALES`, `SPACE-TIME`, `SWITCHES`, `SETUP DEPENDANT`, `GRAVITY`, `PHASE PROPERTIES`), geometry helpers (`IsEllipseCoordinates`, `IsRectangleCoordinates`), and built-in BC templates. Reference: `MDLIB/include/mdoodz.h`, `MDLIB/Setup.c`, `SETS/RiftingChenin.c`, `SETS/RiftingChenin.txt`.

## 2. Rheology and Material Skills

- [x] 2.1 Create `.github/skills/skill-rheology/SKILL.md` — YAML frontmatter (description targeting rheology/flow law/viscosity/creep/plasticity queries) + sections covering: viscosity computation pipeline, power law (dislocation) creep with flow law database entries (cases 1–3 user-defined, 10 Wet Quartzite, 11 Maryland Diabase, 12 Dry Granulite, 19 Westerly Granite, 25 Plagioclase An75, 40 Dry Olivine, 41 Wet Olivine), linear (diffusion) creep, Peierls/exponential creep, grain boundary sliding, constant viscosity, elastic branch (Maxwell model), plastic branch (Drucker-Prager, strain softening), the `mat_prop` struct, density models (equation of state with α, β). Reference: `MDLIB/FlowLaws.c`, `MDLIB/RheologyDensity.c`, `MDLIB/RheologyDensity.h`, `MDLIB/AnisotropyRoutines.c`.
- [x] 2.2 Create `.github/skills/skill-anisotropy/SKILL.md` — YAML frontmatter (description targeting anisotropy/fabric/director/foliation queries) + sections covering: director-based fabric tracking, anisotropy factor definitions, `AnisoFactorEvolv` function, `Y2`/`I2` invariant functions, `ViscosityConciseAniso`, automatic Newton activation, grid-level anisotropy fields (`aniso_factor_n/s`, `d1/d2_n/s`, `angle_n/s`, `FS_AR_n/s`), anisotropy-related `.txt` parameters (`anisotropy`, `aniso_factor`, `aniso_angle`, `ani_fac_max`, `ani_fstrain`), catalogue of 15+ anisotropy scenarios, and reference to the anisotropic rifting paper methodology. Reference: `MDLIB/AnisotropyRoutines.c`, `MDLIB/mdoodz-private.h`.

## 3. Solver and Theory Skills

- [x] 3.1 Create `.github/skills/skill-solvers/SKILL.md` — YAML frontmatter (description targeting solver/convergence/Newton/Stokes/time step queries) + sections covering: main time loop (`RunMDOODZ`), staggered-grid MAC layout, Stokes assembly (`StokesAssemblyDecoupled.c`), penalty method, Picard-to-Newton transition, convergence criteria (`Nparams` struct), line search, thermal solver, SuiteSparse/CHOLMOD direct solver, `DirectSolver` struct, convergence monitoring (`noisy`, `gnuplot_log_res`). Reference: `MDLIB/Main_DOODZ.c`, `MDLIB/StokesAssemblyDecoupled.c`, `MDLIB/Solvers.c`, `MDLIB/ThermalSolver.c`.
- [x] 3.2 Create `.github/skills/skill-physics-theory/SKILL.md` — YAML frontmatter (description targeting equations/conservation/scaling/advection/free surface queries) + sections covering: conservation of momentum (Stokes), mass (continuity), and energy equations, constitutive relations (viscous, visco-elastic, visco-elasto-plastic, anisotropic), non-dimensionalisation (`scale` struct, derived scales), marker-in-cell advection (RK integration, `P2Mastah`, reseeding), free surface algorithm (marker chain, stabilisation, surface processes), pure shear ALE mode. Reference: `MDLIB/Main_DOODZ.c`, `MDLIB/AdvectionRoutines.c`, `MDLIB/FreeSurface.c`, `MDLIB/ParticleRoutines.c`.

## 4. Reference and Catalogue Skills

- [x] 4.1 Create `.github/skills/skill-code-glossary/SKILL.md` — YAML frontmatter (description targeting variable name/symbol/abbreviation/meaning queries) + sections as categorised tables: tensor quantities (`sxxd`→τ'_xx, etc.), material properties (`rho`→ρ, `eta`→η, etc.), grid naming conventions (`_n`=centres, `_s`=vertices, `_0`=previous step), creep abbreviations (`pwl`, `lin`, `exp`, `gbs`, `cstv`), particle/marker fields, solver process stages (function → physical process), scaling/unit conventions, and constants (`zeroC`, `Rg`, BC type codes). Reference: `MDLIB/mdoodz-private.h`, `MDLIB/include/mdoodz.h`.
- [x] 4.2 Create `.github/skills/skill-scenario-gallery/SKILL.md` — YAML frontmatter (description targeting scenario/example/preset/benchmark queries) + scenarios grouped by category: rifting (RiftingBasic, RiftingChenin, RiftingAnisotropy, RiftingMelting, etc.), subduction (SubductionBenchmarkCase2, SubductionMelting, etc.), collision (CollisionZone, CollisionAnisotropy, etc.), shear (ShearTemplate, ShearTemplateAniso, etc.), anisotropy tests (AnisoViscTest_*, AnisotropyDabrowski, etc.), thermal/chemical (ThermalDiffusion, ChemicalDiffusion, etc.), benchmarks (ViscousInclusion, ViscousSinker, VEP_Duretz18, etc.), magmatic (MeltingOverpressure, CoolingChamber, etc.), miscellaneous. Each entry: name, brief description, recommended use. Reference: all `.c` files in `SETS/`.

## 5. Workflow and Validation Skills

- [x] 5.1 Create `.github/skills/skill-visualisation/SKILL.md` — YAML frontmatter (description targeting visualisation/plotting/output/HDF5/Julia/Makie queries) + sections covering: HDF5 output structure and naming, Julia/Makie project setup, available field types (phases, temperature, velocity, pressure, stress, strain rate, viscosity, density, grain size, fabric, topography), overlay options, multi-panel layouts (`Main_Visualisation_Makie_MD7_3x3.jl`), marker trajectory analysis (`Read_Marker_History_MD7.jl`, `Select_Markers_MD7.jl`), batch export, and animation. Reference: `JuliaVisualisation/Main_Visualisation_Makie_MD7.jl`, `MDLIB/HDF5Output.c`.
- [x] 5.2 Create `.github/skills/skill-parameter-validation/SKILL.md` — YAML frontmatter (description targeting validate/check/sanity/range/parameter queries) + checklist-format sections covering: scaling parameter ranges (η 1e15–1e28, L 1e2–1e7, V 1e-15–1e-5, T 1–1e4), domain/resolution validation (Nx/Nz bounds, aspect ratio, CFL), per-phase material property ranges (ρ, G, Cp, k, Qr, C, φ, α, β, aniso_factor), flow law index consistency, switch combination compatibility, boundary condition consistency, time step / Courant number estimation, and a numbered validation checklist workflow. Reference: `MDLIB/include/mdoodz.h` (params struct), `SETS/*.txt` examples.

## 6. Verification

- [x] 6.1 Verify all 10 skill directories exist under `.github/skills/` and each contains a well-formed `SKILL.md` with valid YAML frontmatter (name, description fields present)
- [x] 6.2 Spot-check skill descriptions by mentally matching representative queries: "how to build MDOODZ" → skill-build-and-run, "what does sxxd mean" → skill-code-glossary, "validate my .txt file" → skill-parameter-validation, "rifting setup" → skill-scenario-gallery or skill-model-setup
