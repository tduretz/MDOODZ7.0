## 1. Helpers: per-`aniso_db` analytic inverse of `aniso_delta_fn`

- [x] 1.1 In `MDLIB/AnisotropyRoutines.c`, add `static inline double aniso_delta_inv_hansen(double delta)` returning `γ_eff = −3.96·ln(1 − (δ − 1)/13.132)`. Clamp domain to `δ ∈ [1, 14.132)` with ε = 1e-9; LOG_WARN + clamp on out-of-range.  *(Phase 1: implemented as external-linkage `double aniso_delta_inv_hansen(double delta)` at the top of `AnisotropyRoutines.c`; forward-declared in `mdoodz-private.h`.)*
- [ ] 1.2 Add analytic inverses for every saturating-exponential `aniso_db` case in MDLIB: cases `{1, 2, 3, 4, 5, 6, 7, 8, 9, 13}`. Each helper follows `γ_eff = −γ_e · ln(1 − (δ − 1)/c2)` with mineral-specific `(c2, γ_e, δ_max)` from `MDLIB/FlowLaws.c`. Helper names (post-2026-05-08 substrate — Bruijn 2011 retracted, see `FlowLaws.c:1238-1278`): `aniso_delta_inv_hansen` (1), `aniso_delta_inv_calcite_combined` (2), `aniso_delta_inv_quartz` (3), `aniso_delta_inv_calcite_lowT` (4), `aniso_delta_inv_calcite_highT` (5), `aniso_delta_inv_quartz_blackford` (6), `aniso_delta_inv_hansen_damped` (7), `aniso_delta_inv_quartzmica_bracketed` (8), `aniso_delta_inv_plagioclase_bracketed` (9), `aniso_delta_inv_calcite_superplastic` (13).
- [ ] 1.2b Add the **decay-form** inverse for case 15 (`anisoDelta_QuartzMicaPolyphase`, quartz-perspective): forward is `δ(γ) = 5.385·exp(−γ/15.35) + 1`, so the inverse is `γ_eff = −15.35·ln((δ − 1)/5.385)`. Valid for `δ ∈ (1, 6.385]`. Name: `aniso_delta_inv_quartzmica_polyphase`. Emit a one-shot `LOG_WARN` at init for any phase with this `aniso_db` + `aniso_factor > 1` to alert the user to the reversed monotonicity ("high `aniso_factor` ↔ LOW past strain, opposite to all other cases").
- [x] 1.3 In `MDLIB/include/mdoodz.h::mat_prop`, add `aniso_delta_inv_t aniso_delta_fn_inv[20]` field (function-pointer table mirroring the existing `aniso_delta_fn`). Type alias `typedef double (*aniso_delta_inv_t)(double delta)` in the same header.
- [x] 1.4 In `MDLIB/FlowLaws.c::ReadDataAnisotropy`, populate `mat->aniso_delta_fn_inv[k] = aniso_delta_inv_<case>` alongside the existing `aniso_delta_fn` dispatch. For `aniso_db = 0`, leave NULL.  *(Phase 1: case 1 only — `aniso_delta_inv_hansen` wired. Cases 2-9, 13, 15 stay NULL until Phase 2 task 1.2/1.2b lands. `InputOutput.c:1464` null-inits the whole table.)*
- [ ] 1.5 Bisection fallback `aniso_delta_inv_bisect(δ, aniso_delta_fn_t fwd)` for any future `aniso_db` without closed-form inverse. Bisect `γ_eff ∈ [0, γ_eff_max(FS_AR_CAP)]`, tolerance 1e-9 on δ. Used only as a defensive fallback; emit `LOG_INFO` on activation.
- [ ] 1.6 Unit test (`TESTS/InverseAnisoDeltaTests.cpp`): for each `aniso_db ∈ {1, 2, 3, 4, 5, 6, 7, 9}`, sample `δ ∈ {1.1, 2, 4, 8, c2+1−1e-3}` and assert `|aniso_delta_fn(FS_AR(γ_eff(δ))) − δ| < 1e-9`. (Round-trip).

## 2. Init helper: F-tensor + aniso_delta_fs_prev construction

- [x] 2.1 In `MDLIB/RheologyParticles.c`, factor a helper `static inline void aniso_init_finite_strain_for_marker(int k, int phase, mat_prop *materials, markers *particles)` that:
  (a) computes `γ_eff = aniso_delta_fn_inv[phase](aniso_factor[phase])`;
  (b) computes `FS_AR_init = ((γ_eff + √(γ_eff² + 4)) / 2)²` and `γ_eq = ln(FS_AR_init)`;
  (c) builds `F_canonical = diag(exp(γ_eq/2), exp(−γ_eq/2))`;
  (d) builds `R(θ_F)` with **`θ_F = aniso_angle[phase] − π/2`** (radians; subtract π/2 because `aniso_angle` is the director angle / slip-plane normal, NOT the slip-plane direction — see design D3 corrigendum based on Agent 4 audit of `AnisotropyRoutines.c:924,1081` and `RheologyDensity.c:263`);
  (e) computes `F_init = R(θ_F) · F_canonical · R(θ_F)^T` symbolically (closed form D3 in design.md);
  (f) writes `particles->Fxx[k], Fxz[k], Fzx[k], Fzz[k]`;
  (g) sets `particles->aniso_delta_fs_prev[k] = aniso_factor[phase]`;
  (h) sets `particles->aniso_delta[k] = aniso_factor[phase]` (currently done at lines 64-66, preserve).
- [x] 2.2 Rewrite `InitialiseAnisoDeltaParticles` (`RheologyParticles.c:50-69`) to call `aniso_init_finite_strain_for_marker(k, phase, materials, particles)` for every marker where `phase != -1 && materials->ani_fstrain[phase] == 3 && materials->aniso_delta_fn_inv[phase] != NULL && materials->aniso_factor[phase] > 1.0 + 1e-12`. For the `aniso_factor == 1` case, do nothing (skip the helper; legacy identity behaviour preserved).
- [x] 2.3 Verify the OpenMP `#pragma omp parallel for` at line 60 still applies safely — the helper is fully per-marker, no shared state. Add `firstprivate(materials)` if needed.  *(Phase 1: per-iteration writes are to disjoint particle indices; materials read-only. No `firstprivate` needed; the existing `shared(particles, materials)` clause is sufficient.)*
- [x] 2.4 Add `LOG_INFO("InitialiseAnisoDelta: phase %d ani_fstrain=3 aniso_factor=%g aniso_angle=%g° → γ_eff=%g FS_AR=%g", phase, factor, angle, gamma_eff, FS_AR)` at first invocation per phase (use a per-phase static once-flag). Suppressed for `aniso_factor = 1` phases.

## 3. Out-of-range handling and warnings

- [x] 3.1 In `aniso_delta_inv_*` helpers, if `δ > δ_max[case] − 1e-9`, clamp to `δ_max[case] − 1e-9` and emit `LOG_WARN("aniso_init: phase %d aniso_factor=%g exceeds %s saturation %g; clamped", phase, factor, db_name, delta_max)` (one warn per phase, suppressed on repeated calls).  *(Phase 1: implemented inside `aniso_delta_inv_hansen` — one-shot `static int warned` flag, clamp to `delta_max − 1e-9`. δ_max = 14.132.)*
- [x] 3.2 In `InitialiseAnisoDeltaParticles`, guard against `aniso_factor < 1.0` (legacy invalid setting) — clamp to 1.0 with `LOG_WARN`. (Currently undefined behaviour; tighten.)
- [x] 3.3 In `aniso_init_finite_strain_for_marker`, guard against the bisection fallback running for an `aniso_db = 0` phase — should never happen (`ani_fstrain == 3` requires `aniso_db ≥ 1` per the archived `aniso-delta-relaxation` spec), but emit `LOG_ERR` and skip if it does.  *(Phase 1: `aniso_delta_fn_inv == NULL` → silent skip (legacy fallthrough preserves PartInit defaults). No bisection fallback in Phase 1 (deferred task 1.5), so no LOG_ERR is needed — the NULL check covers the aniso_db = 0 case unambiguously.)*

## 4. F-consumer audit

- [x] 4.1 Audit `Main_DOODZ.c` for any code path between `PartInit` and `InitialiseAnisoDeltaParticles` that reads or writes marker `F`. If any pre-`InitialiseAnisoDeltaParticles` consumer requires `F = I`, document and reorder. (Existing audit at archived `design.md` lines 65-75 confirms there is none, but re-verify.)  *(Phase 1 audit: range `Main_DOODZ.c:208-374` covers PartInit → SetBCs → topo init → PutPartInBox → SetParticles → MinMaxArray → CleanUpSurfaceParticles → CountPartCell → P2Mastah(eta0, noise, T, Cp, k_eff) → InitialiseSolutionFields → ApplyBC → ThermalSteps → SetThermalPert → P2Mastah(T, sxxd, szzd, sxz) → ComputeLithostaticPressure → MeltFractionGrid → InitialiseGrainSizeParticles. None of these reads or writes `particles->Fxx/Fxz/Fzx/Fzz`. `SetParticles` is the user-scenario callback and could in principle set F, but the init helper deliberately overwrites it for ani_fstrain==3 phases with aniso_factor > 1, which is the intended behaviour. SAFE to insert helper at line 374.)*
- [ ] 4.2 Audit `MDLIB/AdvectionRoutines.c` (the per-step F advection) for any assumption that step-0 `F = I`. (Should be none; F advection uses incremental velocity gradients.)
- [ ] 4.3 Audit `MDLIB/AnisotropyRoutines.c::InitialiseDirectorVector` (lines ~1188-1216) and the per-step director advection at `MDLIB/RheologyParticles.c:1647-1657` for any assumption that step-0 `F = I`. (Per Agent 2 audit: the director (nx, nz) is set from `aniso_angle` at init and evolves via Jeffery-like equation independently of F; no F-coupling exists, so this audit should be vacuously satisfied. The original task referenced `MarkerEulerAngles` which does not exist in the current tree.)
- [x] 4.4 Audit `MDLIB/HDF5Output.c` for the centroid `Fxx/Fxz/Fzx/Fzz` write at step 0 — confirm that the non-identity init values flow through to disk without overriding by an "init F = I" reset.  *(Phase 1 audit: `HDF5Output.c:544-568` (gated on `model.finite_strain == 1`) is a pure pass-through — `P2Mastah(particles->Fxx, …)` projects per-marker F to centroid, then `DoubleToFloat` copies for disk. No reset to identity, no override. Non-identity init values flow through.)*
- [ ] 4.5 Audit `MDLIB/ParticleRoutines.c` reseeding paths (lines 710-711, 784-785) — confirm new markers inherit local cell-mean `F` via G2P. The init helper should NOT re-run on reseeded markers (handled by the once-only init-time call, not in the reseed path).
- [ ] 4.6 Audit `MDLIB/FreeSurface.c:155` free-surface marker insertion — same as 4.5.
- [ ] 4.7 Audit `MDLIB/InputOutput.c` breakpoint restart path — confirm `F` is read from the breakpoint without reset. (Per Agent 2 + Agent 7 audits: `InputOutput.c:237-240` reads + `:753-756` writes all four F components positionally, gated on `model->finite_strain == 1`. `aniso_delta` and `aniso_delta_fs_prev` are persisted unconditionally. Post-fix breakpoint round-trip is bit-exact.)

- [x] 4.8 **Pre-existing typo fix** at `MDLIB/FreeSurface.c:166-169` (surface-sediment marker insertion, `AddPartSed`): currently sets `Fxx=1.0, Fxz=0.0, Fzx=1.0, Fzz=0.0` which makes `det F = 0` (singular F). For an isotropic sediment marker the intended values are `Fxx=1.0, Fxz=0.0, Fzx=0.0, Fzz=1.0` (identity). Pre-existing latent defect surfaced by Agent 2 + Agent 7 during this change's audit; fix it here to ensure fresh sediment doesn't immediately hit `FS_AR_CAP` and saturate `aniso_delta_fn`.

- [ ] 4.9 **Transmutation F-state policy** at `MDLIB/ParticleRoutines.c::TransmutateMarkers` (~line 4175): currently only writes `particles->phase[k]` and `particles->dual[k]`. When a marker transmutes from an `ani_fstrain == 3` phase to another phase OR vice versa, the inherited `F`, `aniso_delta`, and `aniso_delta_fs_prev` become inconsistent with the new phase's `aniso_factor`/`aniso_angle`. **Minimum action**: emit a one-shot `LOG_WARN` per `(old_phase, new_phase)` transition pair when either side has `ani_fstrain == 3` with a different `aniso_factor` or `aniso_angle`. Document explicitly that transmutation across anisotropy-state boundaries leaves F/δ governed by the source phase's history (no re-init). Optional follow-up (out of scope): call `aniso_init_finite_strain_for_marker` on transmutated markers entering an `ani_fstrain == 3` phase if the new phase has `aniso_factor > 1` AND the source phase had `aniso_factor != new aniso_factor`.

## 5. CI tests for init-from-finite-strain

- [x] 5.1 New GTest `TESTS/InitAnisoFromFiniteStrainTests.cpp::ConsistencyAtStep0`: for `(aniso_factor, aniso_angle) ∈ {1, 2, 4, 8} × {0, 45, 80, 90}°`, after `InitialiseAnisoDeltaParticles`, assert per marker:
  (a) `|aniso_delta_fn(FS_AR(F_init)) − aniso_factor| < 1e-8` (Agent 8: loosened from 1e-9 — round-trip involves ~6 transcendental evaluations near saturation, 1e-9 is brittle)
  (b) `|aniso_delta_fs_prev − aniso_factor| < 1e-9`
  (c) `|aniso_delta − aniso_factor| < 1e-9`
  (d) the angle of the principal eigenvector of `F_init^T · F_init` matches `(aniso_angle − 90°) ± 1e-7°` (slip-plane direction, per the D3 rotation correction; modulo 180° rotation symmetry)
  (e) `|FS_AR(F_init) − FS_AR_expected| / FS_AR_expected < 1e-9` (sub-assertion localising any failure to the F-construction step vs the helper step)
  (f) `|γ_eff_recovered − γ_eff_input| < 1e-9` (sub-assertion via Hansen forward).
- [x] 5.2 New GTest `ConsistencyAtStep0_aniso_factor1`: for `aniso_factor = 1, aniso_angle ∈ {0, 80}°`, assert `F_init = I` (Frobenius norm of `F_init − I < 1e-12`).  *(Phase 1: Test reimplements the helper math directly (the production helper is `static inline` and not a public symbol), calling production `aniso_delta_inv_hansen` for γ_eff. Round-trip closes via inline Hansen forward.)*
- [x] 5.3 **Promoted to true Hansen-saturation regression** (Agent 8): create `TESTS/SETS/AniFstrainOlivineBoneh_InitFS.txt` with simple shear at `γ̇ = 2.0`, `dt = 0.5`, `Nt = 14` (γ_max ≈ 14, well past Hansen saturation γ_e = 3.96), `aniso_factor = 4`, `aniso_angle = 80°`, `ani_fstrain = 3`, `aniso_db = 1`, `ani_relax_eps_max = -1` (gate OFF), cold T (300 K so τ_relax ≫ run). Assert at steps {5, 10, 14} on the HDF5 output: (a) `δ_max ≤ 14.13 + 1e-6` over the whole domain, (b) `|δ_mean − anisoDelta_HansenOlivine(FS_AR_kinematic)| / δ_mean < 1e-4` (cold-limit collapse to `ani_fstrain == 2`). The pre-fix code yields `δ_max ≈ 17.13` at γ=14, which violates (a) with a factor-3 margin.  *(Phase 1: SET written to `SETS/AniFstrainOlivineBoneh_InitFS.txt` (TESTS/SETS does not exist in the tree) with companion `SETS/AniFstrainOlivineBoneh_InitFS.c` matching the AniFstrainQuartzBlackford convention. NOT registered in `SETS/CMakeLists.txt` yet — manual `add_set(AniFstrainOlivineBoneh_InitFS)` or via `-DSET=AniFstrainOlivineBoneh_InitFS`. Runtime δ_max assertion is left to Phase 2 (no GTest harness yet).)*
- [ ] 5.4 Add fixture `AniFstrainHansen_InitInversionRoundTrip` exercising the inverse helper across all currently-active `aniso_db` cases `{1, 2, 3, 4, 5, 6, 7, 8, 9, 13, 15}`.
- [ ] 5.5 New GTest `ConsistencyAtStep0_aniso_db0_skip`: for `aniso_factor = 4, aniso_angle = 45°, ani_fstrain = 3, aniso_db = 0`, assert `F_init = I` (Frobenius < 1e-12), `aniso_delta = aniso_factor`, `aniso_delta_fs_prev = 1.0` (legacy fallthrough, no helper invocation). Verify `LOG_ERR` is NOT emitted (this configuration is legal-but-degenerate, not an error).
- [ ] 5.6 New GTest `ConsistencyAtStep0_OverSaturationClamp`: for `aniso_factor ∈ {20, 100}` on `aniso_db = 1` (δ_max = 14.132), assert helper clamps `aniso_delta` to `14.132 − 1e-9`, one-shot `LOG_WARN` emitted (capture LOG to buffer), `F_init` finite, `aniso_delta_fn(FS_AR(F_init)) ≈ 14.132 − 1e-9`.
- [ ] 5.7 New GTest `ConsistencyAtStep0_NegativeFactorClamp`: for `aniso_factor = 0.5`, assert clamp to 1.0, `F_init = I`, `aniso_delta = aniso_delta_fs_prev = 1.0`, one-shot `LOG_WARN`.
- [ ] 5.8 New GTest `ConsistencyAtStep0_Angle90Sign`: for `aniso_factor = 4, aniso_angle = 90°`, assert (a) `F_init` finite, (b) `|det(F_init) − 1| < 1e-12`, (c) signed γ_eq recovered from `0.5·ln(λ_max(F^T F))` equals constructed γ_eq to 1e-9 (NOT just absolute value — guards against eigenvector sign-flip degeneracy hidden by 5.1(d)'s ±1e-7° tolerance).
- [ ] 5.9 (covered by promoted 5.3 above — Hansen-saturation regression.) — placeholder; collapse if redundant.
- [ ] 5.10 New GTest `ColdQuiescentInvariant`: `bkg_strain_rate = 0`, Nt = 20, `aniso_factor = 4, aniso_angle = 60°, ani_fstrain = 3, aniso_db = 1`, cold T. Assert `|δ(step k) − aniso_factor| < 1e-12` for k ∈ {1, 5, 10, 20} and `‖F(step k) − F_init‖ < 1e-12` (no drift under zero deformation). CI version of §7.4.
- [ ] 5.11 New GTest `BreakpointRestartRoundTrip`: 6-step sim with `aniso_factor = 4, ani_fstrain = 3, aniso_db = 1`, write breakpoint at step 3, restart, run to step 6. Compare per-marker `aniso_delta`, `aniso_delta_fs_prev`, `Fxx/Fxz/Fzx/Fzz` against a reference no-restart trajectory; assert max diff < 1e-12.
- [ ] 5.12 New GTest `ReseedInheritsFiniteStrain`: configure a low-marker-count cell that triggers reseeding within ~3 steps; assert new markers inherit closest-neighbour F (Frobenius distance < 1e-8 in stable flow), and that their initial `aniso_delta_fs_prev` matches `aniso_delta_fn(FS_AR(F_inherited))` — NOT re-initialised to `aniso_factor[phase]`. Otherwise the +offset re-enters at every reseed event.
- [ ] 5.13 (folded into 5.1 above — sub-assertions e/f localise round-trip failures.)
- [ ] 5.14 New GTest `BisectionFallbackConvergence`: register a synthetic forward `aniso_delta_fn_test(γ_eff) = 1 + 3·tanh(γ_eff)` with no analytic inverse, invoke `aniso_delta_inv_bisect` for δ ∈ {1.5, 2.5, 3.9}, assert `|forward(bisect(δ)) − δ| < 1e-9` and a `LOG_INFO` is emitted on activation (per task 1.5).
- [ ] 5.15 New GTest `Case15DecayFormInverse`: for `aniso_db = 15` (`anisoDelta_QuartzMicaPolyphase`, decay form `δ = 5.385·exp(−γ/15.35) + 1`), `aniso_factor = 4`, `aniso_angle = 45°`, assert (a) inverse yields a positive finite γ_eff, (b) round-trip `aniso_delta_fn(FS_AR(γ_eff)) ≈ aniso_factor` to 1e-8, (c) one-shot `LOG_WARN` is emitted about reversed monotonicity. Aim is to surface case-15 semantics if a user activates it.

## 6. Documentation and migration

- [x] 6.1 Update `MDLIB/RheologyParticles.c` header comment of `InitialiseAnisoDeltaParticles` (lines 50-69) to reflect the new F-tensor + aniso_delta_fs_prev consistency invariant.
- [x] 6.2 Update the inline comment block at `MDLIB/RheologyParticles.c:744-765` (the operator-split documentation in `FiniteStrainAspectRatio`) to drop the "+ (aniso_delta_0 − aniso_delta_fs_prev_0)" caveat note (this change makes it always 0).
- [ ] 6.3 Update `MDLIB/AnisotropyRoutines.c` lines 130-200 (the design-doc comment block on the DiSRX modelling leap) to reference the new init-from-finite-strain capability for inherited-fabric setups.
- [ ] 6.4 In each currently-existing rifting config that sets `aniso_factor > 1` on an `ani_fstrain == 3` phase, add a one-line comment `/ init-from-finite-strain: F_init encodes γ_eff = aniso_delta_inv(aniso_factor) at (aniso_angle − 90°)`. Per the SET-inventory audit (Agent 5) the affected paths are:
  - `SETS/RiftingRomanAniso3.txt`
  - `cmake-exec/RiftingRoman/RiftingRomanAniso3.txt`
  - `cmake-exec/RiftingRoman/RiftingRomanAniso3_smoke.txt` (if still present)
  - `cmake-exec/RiftingRoman/debug_run/RiftingRomanAniso3.txt`
  - `cmake-exec/RiftingRoman/debug_run/RiftingRomanSmoke.txt` (if still present)
  - `cmake-exec/RiftingRoman/run_aniso3_v2/RiftingRomanAniso3_v2.txt`
  - any new `cmake-exec/RiftingRoman/run_aniso3_v3*/...` config staged for the post-fix smoke / production runs

  Also add to `cmake-exec/RiftingRoman/run_aniso3_init1/RiftingRomanAniso3_init1.txt`: `/ aniso_factor=1 → init-from-finite-strain helper skipped (F_init=I preserved)`. No semantic change in any of the above; documentation only.
- [ ] 6.5 BREAKING note in the project changelog / OpenSpec change archive: "ani_fstrain == 3 with aniso_factor > 1 now initialises F to a non-identity tensor; prior runs that relied on the +offset above Hansen ceiling will produce δ ~aniso_factor−1 lower in actively-deformed shear bands. This is a bug fix."
- [ ] 6.6 Add a snippet to `~/.claude/reading/cooking-mdoodz-models/primer.md` (or successor) noting the cookbook for "I want inherited cratonic fabric" → "set `aniso_factor`, `aniso_angle`, and the init handles F automatically."

## 7. Sensitivity sweep + comparison runs

- [ ] 7.1 Re-run `RiftingRomanAniso3_v2` with the new init at 400×210 to ~5 Ma. Compare `δ_max(t)` against the legacy v2 run (currently archived as `run_aniso3_v2/`). Expected: `δ_max ≤ 14.13` everywhere post-fix; cold-static far-field `δ ≈ 4` preserved; shear-band δ saturates at Hansen (≈14) instead of overshooting to 16.
- [ ] 7.2 Run a sweep over `aniso_factor ∈ {1, 2, 4, 8, 12}` on `RiftingRomanAniso3_v2` for `Nt = 200` (~1.4 Ma) to characterise how the strain-band δ peak depends on inherited fabric magnitude. Plot `δ_max(aniso_factor, t)`.
- [ ] 7.3 Document the comparison figure in the archived change directory (`results/aniso_factor_sweep_2026-05-XX.md` or similar).
- [ ] 7.4 Cold-quiescent invariant test: run `RiftingRomanAniso3_v2` with `bkg_strain_rate = 0` (no deformation) for `Nt = 100`. Cold cells SHALL maintain `δ = aniso_factor` exactly (no drift). Hot cells SHALL relax `δ → 1` exponentially as before.

## 8. Optional follow-ups (out-of-scope for this change, documented for tracking)

- [ ] 8.1 (Out of scope) Extend init-from-finite-strain to `ani_fstrain ∈ {1, 2}` phases. Currently the helper is gated to `ani_fstrain == 3` only.
- [ ] 8.2 (Out of scope) Add per-marker HDF5 output of `aniso_delta_fs_prev` for post-mortem audit of the operator-split state.
- [ ] 8.3 (Out of scope) Generalise `F_canonical` to support simple-shear initialisation in addition to pure-shear, for cases where the user wants to encode inherited fabric *with* a specific initial vorticity.
- [ ] 8.4 (Out of scope) Lab-validation test: simulate Hansen+2014 torsion experiment with `aniso_factor > 1` initial condition + `ani_fstrain == 3` + strain-rate gate disabled, verify δ(γ) curve matches Hansen+2014 Fig 3 within fit uncertainty.
