include(AddSet.cmake)

if (SET)
    add_set(${SET})
else()
    add_set(ShearHeatingDuretz14)
    add_set(StrainLocalization_SH_GSE)
    add_set(ViscousInclusion)
    add_set(PinchSwellGSE)
    add_set(PinchSwellGSEAniso)
    add_set(ShearTemplate)
    add_set(ShearTemplateAnisotropy)
    add_set(RiftingChenin)
    add_set(RiftingBasic)
    add_set(RiftingRoman)
    add_set(TopoBenchCase1)
    add_set(AnisotropyLayer)
    add_set(RiftingCheninAniso)
    add_set(SimpleShearAnisoHomo)
    # ani_fstrain=2 lab-validation SETs (Hansen olivine, calcite, quartz).
    add_set(AniFstrainCalcitePieri)
    add_set(AniFstrainCalciteBruijn)
    add_set(AniFstrainCalciteSchuster)
    add_set(AniFstrainQuartzPennacchioni)
    add_set(AniFstrainQuartzBlackford)
    # ani_fstrain=3 Phase-1 F-init regression set (inherited fabric).
    add_set(AniFstrainOlivineBoneh_InitFS)
    # ani_fstrain visual-tests box (driven by VISUAL_TESTS/aniso_fstrain/runners/*.py).
    add_set(AnisoFstrainBox)
    add_set(ShearInclusionAniso)
    add_set(VEP_Duretz18)
    add_set(Multilayers)
    add_set(Shrinking)
    add_set(RiverTom)
    add_set(AnisoHomoVEVP)
    add_set(CollisionIra)
    add_set(NeckingReview)
    add_set(CollisionPolarCartesian)
    add_set(AnneloreSubduction)
    add_set(ThanushikaSubduction)
    add_set(SubductionMelting)
    add_set(RiftingCombinedYield)
    add_set(RiftingComprehensive)
endif()

